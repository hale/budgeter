- if @expenses.any?
  %h2
    = "#{@category.name.capitalize} expenses"
  = render :partial => 'expenses/expenses'

= link_to 'Edit', edit_category_path(@category)
\|
= link_to 'Back', categories_path
