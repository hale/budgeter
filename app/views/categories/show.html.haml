%p#notice= notice

- if @transactions.any?
  %h2
    = "#{@category.name.capitalize} transactions"
  = render :partial => 'transactions/transactions'

= link_to 'Edit', edit_category_path(@category)
\|
= link_to 'Back', categories_path
