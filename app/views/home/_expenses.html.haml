%table#expenses
  %tr
    %th Day
    %th Amount
    %th Description
    %th Category
    %th
    %th

  - expenses.each do |expense|
    %tr
      %td= pretty_date expense.date
      %td= humanized_money_with_symbol expense.amount
      %td= expense.description.truncate(25) if expense.description
      %td= expense.category.try(:name)
      %td= link_to 'edit', edit_expense_path(expense)
      %td= link_to 'destroy', expense, :method => :delete, :data => { :confirm => 'Are you sure?' }
