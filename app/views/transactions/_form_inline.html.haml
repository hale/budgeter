= form_for @transaction do |f|
  - if @transaction.errors.any?
    #error_explanation
      %h2= "#{pluralize(@transaction.errors.count, "error")} prohibited this transaction from being saved:"
      %ul
        - @transaction.errors.full_messages.each do |msg|
          %li= msg

  %div
    %fieldset
      -#%legend Transaction
      %div
        = f.label :date
        = f.text_field :date, :value => (@transaction.date.to_s(:short) if @transaction.date)
      %div
        = f.label :amount
        = f.text_field :amount, :autofocus => true
      %div
        = fields_for (@transaction.category || @transaction.build_category) do |category_fields|
          = category_fields.label :category_name
          = category_fields.text_field :name
      = f.submit 'Save'#, :class => "hide"
